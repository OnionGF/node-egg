exports.mongoose = {
  enable: true,
  package: 'egg-mongoose'
}

// 校验
exports.validate = {
  enable: true,
  package: 'egg-validate'
}

// 跨域
exports.cors = {
  enable: true,
  package: 'egg-cors'
}
